# MVP rules

- Keep this project isolated from running services.
- Use SQLite foreign keys for the signal-to-revenue chain.
- Never mark an external provider PASS without a provider response.
- Local publisher data is valid only as a publication smoke test.
- Do not add automated social posting, ad clicks, CAPTCHA bypass, or ranking manipulation.
